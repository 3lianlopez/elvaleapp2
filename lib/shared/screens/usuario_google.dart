import 'package:flutter/material.dart';

class UsuarioGoogle extends StatelessWidget {
  const UsuarioGoogle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}